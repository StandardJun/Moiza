name: moiza
description: "모이자(Moiza) - 모임 출석 체크와 벌금 자동 계산 앱"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0

  # State Management
  provider: ^6.1.0

  # Routing
  go_router: ^14.0.0

  # Ads (추후 활성화)
  # google_mobile_ads: ^5.0.0

  # Utils
  intl: ^0.19.0
  shared_preferences: ^2.2.0
  uuid: ^4.0.0

  # UI
  flutter_svg: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
